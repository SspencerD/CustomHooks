#useFetch

Ejemplo de uso:
```
const url = 'endpoint de una Api' 'https://example/v2/?apikey=4848393/users/2'
const { data: null , loading:true , error:null } = useFetch(url);

```

Con esto puedo iniciar la extracción de datos de una APi.

